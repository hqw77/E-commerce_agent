# 云效代码获取操作手册
这份手册用于帮助你从云效/Codeup 获取“跨境电商 Agent 项目课”的代码，并在本地打开。完成后，你应该能在电脑上看到代码仓，并在 Trae CN 中打开它，继续按照项目课入门指引和运行手册学习。

本文只讲“如何拿到代码”。代码运行、模型 Key 配置、Docker/Python/Node 环境安装，请继续查看代码仓内的 `doc/运行手册.md`。

## 一、准备工作
开始前，请确认你已经具备：

+ 电脑已安装 Git；
+ 如果还没有安装，也可以先使用“下载代码”方式获取代码。

平台地址：

[https://codeup.aliyun.com](https://codeup.aliyun.com)

打开地址后，如果没有登录，会跳转到登录页面。你可以使用支付宝扫码、阿里云账号或方提供的账号方式登录。

![](https://cdn.nlark.com/yuque/0/2026/png/40749310/1783446279203-d7fbdf4f-1be3-4b5f-8b6d-28d33326f8a9.png)

登录后需点击邀请并经过助教确认后才能进入代码仓：<font style="background-color:#FBDE28;">链接参考群公告</font>

如看不到代码仓，通常是账号还没有加入对应组织，或当前登录账号不是授权账号。请先确认登录账号，再联系助教处理权限。

## 二、进入代码仓
登录成功后进入云效主页面，在左侧导航栏找到 **代码库**，页面中部会展示你有权限访问的代码库列表。

![](https://cdn.nlark.com/yuque/0/2026/png/40749310/1783446279332-3ba07091-b889-49a3-82ff-e2e4a762bff1.png)

在代码库列表中找到代码仓。仓库名称为

```latex
xiaozhe-E-commerce
```

点击仓库名称进入详情页。进入仓库后，点击页面右上方的 **克隆/下载** 按钮。

![](https://cdn.nlark.com/yuque/0/2026/png/40749310/1783446279397-2ff439a8-4eb9-480c-81c1-dc19314ef886.png)

## 三、推荐方式：克隆代码到本地
推荐使用 Git 克隆代码。这样以后代码有更新时，可以在本地执行 `git pull` 获取最新版本。



### 3.1 复制 HTTPS 克隆地址
进入代码仓后，点击右上方 **克隆/下载**，切换到 **HTTPS**。在 HTTPS 页签中复制代码仓的克隆地址，地址通常类似：

```latex
https://codeup.aliyun.com/xxx/xiaozhe-E-commerce.git
```

后面执行 `git clone` 时，会用到这个地址。

### 3.2 准备 HTTPS 克隆账号和密码
使用 HTTPS 地址克隆时，Git 会要求你输入账号和密码。这里用的是云效 Codeup 的 **HTTPS 克隆账号** 和 **HTTPS 密码**，不一定等同于你登录云效网页时使用的账号密码。

你可以通过两种方式找到或设置：

方式一：在代码仓页面查看

1. 进入代码仓。
2. 点击右上方 **克隆/下载**。
3. 切换到 **HTTPS**。
4. 点击 **立即查看**。
5. 首次需要绑定邮箱才可以设置密码。
6. 及时保存设置好的账号和密码；如果之后忘记密码，需要重新设置 HTTPS 密码。

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/40749310/1783446279463-8625138c-6378-4cc1-8c92-2d12c635e3c1.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/40749310/1783477353087-ffaba9bf-133c-40fb-9cd6-f9e617e6cd39.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/40749310/1783446279531-7756a3a5-4b0a-4bc0-a32f-ec82b1fe606b.png)

方式二：在个人设置中查看或设置

1. 点击云效页面右上角头像。
2. 进入 **个人设置**。
3. 找到 **HTTPS 密码**。
4. 首次需要绑定邮箱才可以设置密码。
5. 及时保存设置好的账号和密码；如果之后忘记密码，需要重新设置 HTTPS 密码。

后续终端提示输入账号密码时：

<font style="background-color:#FBDE28;">【注意：账号不是用户名，是克隆账号】</font>

<!-- 这是一张图片，ocr 内容为：个人设置 克隆账号 账户设置 24664666128 习 个人信息 第三方绑定 克隆密码 个人访问令牌 绑定邮箱后,可修改密码 代码管理 SSH公钥 GPG公钥 HTTPS密码 组织管理 已加入组织 -->
![](https://cdn.nlark.com/yuque/0/2026/png/28539630/1783480038449-24277280-6d02-415f-9f92-7d2dfc577ed1.png)

```latex
Username for 'https://codeup.aliyun.com':
```

这里输入云效显示的 **克隆账号**。

```latex
Password for 'https://...':
```

这里输入你设置的 **HTTPS 密码**。终端输入密码时通常不会显示字符，这是正常现象，输入完直接按回车即可。

请不要把 HTTPS 密码发到聊天窗口、截图、群或公开文档中。

### 3.3 可选：登记 SSH Key
如果你希望后续拉取代码时不再输入 HTTPS 账号密码，也可以使用 SSH 方式克隆。使用 SSH 前，需要先把你电脑上的 SSH 公钥登记到云效账号里。

如果你只是第一次获取代码，HTTPS 方式已经够用；如果你熟悉命令行，或后续会多次 `git pull` 更新代码，可以配置 SSH。

#### 3.3.1 生成 SSH Key
打开终端，执行：

```bash
ssh-keygen -t ed25519 -C "你的邮箱@example.com"
```

执行后，终端会询问保存位置。一般直接按回车，使用默认路径即可：

```latex
~/.ssh/id_ed25519
```

如果继续询问是否设置 passphrase，可以直接按回车跳过，也可以自己设置一个本机密码。

生成完成后，你的电脑里会出现两个文件：

```latex
~/.ssh/id_ed25519
~/.ssh/id_ed25519.pub
```

其中：

+ `id_ed25519` 是私钥，不要发给任何人，也不要贴到聊天、截图或文档里。
+ `id_ed25519.pub` 是公钥，可以登记到云效。

#### 3.3.2 复制 SSH 公钥
macOS / Linux 可以执行：

```bash
cat ~/.ssh/id_ed25519.pub
```

复制终端输出的整行内容。内容通常以 `ssh-ed25519` 开头，以你的邮箱或备注结尾。

Windows 如果使用 Git Bash，也可以执行：

```bash
cat ~/.ssh/id_ed25519.pub
```

如果使用 PowerShell，可以执行：

```powershell
Get-Content $env:USERPROFILE\.ssh\id_ed25519.pub
```

只复制 `.pub` 公钥文件里的内容，不要复制没有 `.pub` 后缀的私钥文件。

#### 3.3.3 在云效登记 SSH 公钥
在云效页面中按下面步骤操作：

1. 点击右上角头像。
2. 进入 **个人设置**。
3. 找到 **SSH 公钥**。
4. 点击 **添加公钥**。
5. 把刚才复制的 `id_ed25519.pub` 内容粘贴到公钥内容中。
6. 公钥标题可以填写 `xiaozhe-course-你的电脑名称`，方便以后识别。
7. 如果页面要求选择作用范围，只学习和拉取代码时可以选择 **只读**。
8. 点击 **添加** 。

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/40749310/1783446279595-06f86993-2bc8-467b-babc-78cf8aab8401.png)

登记完成后，回到代码仓页面，点击 **克隆/下载**，切换到 **SSH**，复制 SSH 地址：

```latex
git@codeup.aliyun.com:xxx/xiaozhe-E-commerce.git
```

后续克隆时使用：

```bash
git clone git@codeup.aliyun.com:xxx/xiaozhe-E-commerce.git
```

如果 SSH 克隆失败，先确认你复制的是 SSH 地址，不是 HTTPS 地址；再确认云效里登记的是 `.pub` 公钥内容，而不是私钥内容。

### 3.4 在本地选择保存位置
建议把代码放在一个路径简单、没有中文和特殊符号的目录下。例如：

macOS / Linux：

```bash
mkdir -p ~/Courses
cd ~/Courses
```

Windows：

```latex
D:\Courses
```

不要把代码放在系统目录、网盘同步目录或路径特别深的位置。这样后续启动服务和排查问题会更简单。

### 3.5 执行克隆命令
在终端中执行：

```bash
git clone 你复制的云效仓库地址
```

例如：

```bash
git clone https://codeup.aliyun.com/xxx/xiaozhe-E-commerce.git
```

如果你使用的是 SSH 地址，则类似：

```bash
git clone git@codeup.aliyun.com:xxx/xiaozhe-E-commerce.git
```

如果你使用 HTTPS 地址，终端可能会依次提示输入用户名和密码。请按 `3.2 准备 HTTPS 克隆账号和密码` 填写云效的克隆账号和 HTTPS 密码。

克隆完成后，会在当前目录下生成一个代码仓文件夹。

### 3.6 已经克隆过代码后，如何拉取最新代码
如果你之前已经用 Git 克隆过代码，后续代码有更新时，不需要重新克隆。打开终端，先进入你本地的代码仓目录，再执行拉取命令。

例如你的代码放在 `~/Courses/xiaozhe-E-commerce`：

```bash
cd ~/Courses/xiaozhe-E-commerce
git pull
```

如果你的代码放在 Windows 的 `D:\Courses\xiaozhe-E-commerce`，可以先在文件资源管理器中进入这个文件夹，在空白处右键打开终端，然后执行：

```bash
git pull
```

如果你使用 HTTPS 地址克隆，拉取时可能会再次要求输入用户名和密码：

+ 用户名：输入云效显示的 **HTTPS 克隆账号**。
+ 密码：输入你在云效 **HTTPS 密码** 页面设置的 HTTPS 密码，不是网页登录密码。

如果你使用 SSH 地址克隆，正常情况下执行 `git pull` 时不需要输入 HTTPS 账号密码。

拉取完成后，如果终端提示 `Already up to date.`，说明你本地代码已经是最新版本；如果显示更新了若干文件，说明最新代码已经同步到本地。

如果 `git pull` 提示本地文件有修改，先不要直接删除文件或强制覆盖。通常可以先执行：

```bash
git status
```

查看哪些文件被修改过。若这些修改是你自己为了学习做的实验代码，建议先备份或单独保存；如果你不确定怎么处理，可以把终端提示交给我们的skill进行处理。

## 四、备选方式：直接下载代码
如果你暂时不会使用 Git，或者电脑还没有安装 Git，可以先用云效提供的下载功能获取代码。

点击 **克隆/下载** 后，在弹出窗口中选择下载代码。一般可以选择 ZIP 或 TAR.GZ：

| 格式 | 说明 |
| --- | --- |
| ZIP | Windows、macOS、Linux 都容易解压，推荐优先选择 |
| TAR.GZ | macOS / Linux 常见格式 |


<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/40749310/1783446279667-01b98091-0f98-4de5-9c5b-a8fb2991acce.png)

下载并解压后，你同样会得到一个代码仓目录。

需要注意：下载方式不会保留 Git 更新能力。后续如果代码有更新，你通常需要重新下载或改用 Git 克隆。

## 五、确认代码仓是否正确
无论你是克隆还是下载，最终都需要确认打开的是代码仓根目录。

正确的代码仓里通常能看到：

```latex
agent-course-versions/
frontend/
ecommerce-backend/
requirements.txt
docker-compose.infra.yml
release-version.json
```

这些内容分别对应：

| 路径 | 作用 |
| --- | --- |
| `agent-course-versions/` | 每一课的 Agent 代码快照、README 和场景材料 |
| `frontend/` | 调试台 |
| `ecommerce-backend/` | 跨境电商公司的业务后端 |
| `requirements.txt` | Agent 后端依赖 |
| `docker-compose.infra.yml` | 启动基础设施和电商业务后端的 Compose 文件 |
| `release-version.json` | 当前代码发布身份，`releaseVersion` 按发布日期生成 |

你可以直接打开仓库根目录的 `release-version.json`，确认自己拿到的发布版本。例如：

```json
{
  "releaseVersion": "2026-08-06"
}
```

这个日期只表示你本地代码的发布身份。要判断云效上是否还有更新，先执行 `git pull`；如果文件不存在，通常说明本地还是尚未加入发布身份的旧版代码仓。

如果你看到的是上一级目录，里面只有一个代码仓文件夹，请进入这个文件夹后再打开。

如果你没有看到 `agent-course-versions/`、`frontend/`、`ecommerce-backend/`，说明可能打开错了目录，或下载/克隆没有完成。

## 六、在 Trae CN 中打开代码仓
打开 Trae CN，选择 **打开文件夹**，然后选择刚才克隆或解压出来的代码仓根目录。（要选中根目录）

![](https://cdn.nlark.com/yuque/0/2026/png/40749310/1783478323126-1d57dbfe-110d-4841-91eb-422aa03137e0.png)

打开后，你应该能在 Trae 文件树中看到：

```latex
agent-course-versions/
frontend/
ecommerce-backend/
```

当前代码仓已经预置小哲助手 Skill，常见目录是：

```latex
.trae/skills/xiaozhe-release-course-assistant/
.agents/skills/xiaozhe-release-course-assistant/
```

如果 Trae 正常识别项目 Skill，后续你可以在 AI 对话框中输入：

```latex
使用小哲助手，请确认当前打开的目录是不是跨境电商 Agent 项目课代码仓。
```

也可以使用 `/` 方式调用：

```latex
/xiaozhe-release-course-assistant 请确认当前打开的目录是不是跨境电商 Agent 项目课代码仓。
```

## 七、后续应该看什么
代码获取完成后，请继续查看：

+ 《跨境电商 Agent 项目课入门指引》：了解代码仓、Skill 安装与第一次学习顺序。
+ 运行手册：配置环境、模型 Key、启动电商后端、启动 Agent 后端和前端调试台。

建议第一次打开代码仓后，先不要急着启动服务。可以先让小哲助手检查目录结构，再按照运行手册一步步准备环境。

## 八、常见问题
### Q1：我登录云效后看不到代码仓怎么办？
先确认你登录的是授权账号。如果账号没错但仍然看不到仓库，通常是权限还没有开通，请联系助教处理。

### Q2：应该用 HTTP 还是 SSH？
第一次使用建议先用 HTTP/HTTPS，复制地址后直接 `git clone`，按提示输入云效 HTTPS 克隆账号和密码。

如果你不想每次输入 HTTPS 账号密码，或后续会经常 `git pull` 更新代码，可以按本文 `3.3 可选：登记 SSH Key` 配置 SSH，然后使用 SSH 地址克隆。

### Q3：克隆时提示需要用户名和密码怎么办？
使用 HTTPS 克隆时，这是正常提示。

+ 用户名：输入云效 **HTTPS 密码** 页面显示的克隆账号。
+ 密码：输入你在云效中设置的 **HTTPS 密码**。

如果一直提示账号或密码错误，先回到云效 **个人设置** > **HTTPS 密码** 重新设置 HTTPS 密码，再重新克隆。

如果你确认账号密码正确，但电脑仍然反复报错，可能是本机缓存了旧凭据。macOS 可以检查“钥匙串访问”，Windows 可以检查“凭据管理器”，删除旧的 `codeup.aliyun.com` 相关凭据后再试。

### Q4：下载 ZIP 可以吗？
可以。ZIP 适合先快速拿到代码，但不方便后续更新。代码如果有更新，推荐改用 Git 克隆或重新下载最新 ZIP。

### Q5：我应该打开哪个目录？
打开包含 `agent-course-versions/`、`frontend/`、`ecommerce-backend/` 的那一层目录。不要只打开 `agent-course-versions/`，也不要打开它的上一级空目录。

### Q6：代码仓里没有完整正文，是不是缺文件？
不是。代码仓主要用于发放可运行代码、调试台、场景材料和启动文件。正式内容和补充手册请查看平台和语雀共享文件目录。
